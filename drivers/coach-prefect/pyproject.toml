[project]
name = "coach-prefect"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "prefect>=3.0",
    "docker>=7.0",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["main"]
